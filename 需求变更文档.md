## 需求变更文档

原需求：[需求文档](./需求文档.md)


## 需求变更-1

增加如下自定义配置：

- 复制的接口信息，增加接口请求时间，耗时；去掉 完整地址 字段；
- 自定义过滤接口前缀：本插件会在 web 页面记录所有接url请求，但是我想反馈的接口只是对应的 接口 url，比如 /api/saas/ 开头的；增加相关配置，允许用户自定义对应的接口前缀配置
- 可增加自定义属性：比如用户想配置一下反馈人，配置后所有复制信息后面的 自定义字段中都包含 用户配置的反馈人，比如：反馈人-张三


## 需求变更-2

对于选中展示的接口，我想通过 url 匹配查找到对应的 apifox 接口地址，即，当选中接口信息复制的时候，会使用 url 去 apifox 接口中匹配，获取对应的 url 在 apifox 的预览地址；拿到地址后，在复制信息中的接口信息里，每个接口新增一个字段：

- apifox: [对应 apifox 的地址]


补充需求点：

在 popup 设置功能，新增一个配置本地 apifox 导出地址；配置后保存地址，检测返回是否正常，若正常，则后续不需要变更了；若本请求未返回，则提示，可能原因为 本地 apifox 软件没有打开；

因为 apifox 导出的数据比较大，不要存在本地，放在内存中即可；

当内存中已有 apifox 接口信息时，在 popup 顶部增加一个 小绿灯 + apifox ，表示 apifox 接口信息已就绪；右侧增加一个刷新按钮图标，因为 接口信息可能随时变动，可刷新更新接口信息；

---

apifox 说明：这是一个 类似 postman 的接口文档软件；可导出所有接口在本地使用 json 的形式展示；因此，在本地可以获取到该导出的所有接口信息；拿到信息可以获取需要的内容；比如 tag，出入参数等；

本次需求关键点：

- 本地导出的 url：http://127.0.0.1:4523/export/openapi?projectId={PROJECT_ID}&specialPurpose=openapi-generator
- 字段获取："x-run-in-apifox" 字段：示例：https://app.apifox.com/project/{projectId}/apis/api-{apiId}-run，注意，这里需要去掉最后的 `-run`

结果为：https://app.apifox.com/project/{projectId}/apis/api-{apiId}

类似导出 GET 类型的接口 的 json 示例：

```
 // 略，请在 apifox 导出任意一个接口，选择 包含 apifox 目录
```