# Quick-copy-ext 需求文档

## 项目说明

当前仓库基于 `vite-web-extension` 模板项目开发，用于实现一个 Chrome 扩展插件。

### 技术改造方向

- 包管理器使用 `pnpm`
- UI 组件库使用 `antd`
- npm 源使用淘宝源

## 需求背景

在前后端联调、测试或线上问题排查过程中，发现 Web 页面异常时，通常需要将问题信息反馈给对应的前端或后端开发同学。为了便于快速定位和复现问题，反馈内容通常需要包含以下关键信息：

- 当前页面的 URL 和标题
- 异常接口的 URL 和请求方法
- 接口中的关键 Header 信息，例如响应头中的 `traceId`
- 页面截图

目前这类信息的收集和整理主要依赖人工手动完成，容易出现信息缺失、描述不统一、反复补充沟通等问题，导致排查效率较低。

因此，需要开发一个 Chrome 扩展插件，将问题反馈流程标准化，支持一键收集并复制关键信息到剪贴板，帮助开发人员更快地定位和复现问题。

## 需求目标

开发一个用于异常问题反馈的 Chrome 扩展插件，能够从当前页面及其接口请求记录中提取必要信息，并按照统一格式生成反馈内容，供用户快速复制使用。

## 当前需求结论

本文档已合并初版需求、需求变更文档以及后续交互讨论结论，以下内容作为当前版本的最终需求说明。

## 插件核心功能

插件需要支持一键复制异常反馈信息，复制内容建议采用如下格式：

```text
=== 异常接口反馈

Web 信息：

- 页面 URL：
- 页面标题：

异常接口信息：

- POST /api/saas/user/queryList
- traceId: 123456789（若不存在则显示 N/A）
- 状态码：200
- 请求时间：12:30:25
- 耗时：248 ms

截图信息（如实现成本较高，可暂时不纳入首期范围）：

- [截图]

自定义字段：

- 反馈人-张三
- 环境-测试环境

其他备注说明：

=== From Quick Copy Ext
```

## 复制反馈说明

复制后的异常接口信息需要至少包含以下字段：

- 请求方法
- 接口路径
- `traceId`
- 状态码
- 请求时间
- 接口耗时

以下字段不再作为当前版本复制内容的一部分：

- 完整地址

复制成功后，插件需要给用户明确反馈，例如顶部成功提示或状态提示，避免用户不确定是否复制成功。

## UI 交互说明

插件采用 `popup` 交互形式。

安装插件后，用户点击插件图标即可打开弹窗界面。默认状态下，弹窗展示主功能区域，包括：

- 当前页面基础信息
- 当前页面捕获到的接口请求列表
- 备注输入区域
- 复制按钮

用户可以在接口列表中勾选一个或多个目标接口，点击“复制”按钮后，将当前页面信息、所选接口信息以及其他辅助信息按约定格式写入剪贴板。

### 设置交互

弹窗右上角需要提供“设置”按钮，用于切换进入设置视图。

进入设置视图后，界面只展示与配置相关的字段及操作按钮，不展示页面信息、接口列表、备注输入等主功能内容，避免因信息过多影响用户操作。

设置视图中需要包含以下内容：

- 接口前缀过滤配置
- 自定义字段配置
- 保存按钮
- 取消或关闭按钮

用户点击保存后，需要明确提示：

- 配置已保存
- 需要刷新当前页面后，新配置才会生效

## 配置能力说明

### 1. 接口前缀过滤

插件会记录当前页面中的接口请求，但实际反馈时，用户通常只关心特定前缀下的接口，例如 `/api/saas/`。

因此，插件需要支持用户自定义接口前缀过滤规则：

- 支持配置一个或多个接口前缀
- 仅展示符合前缀规则的接口请求
- 复制时仅带出符合过滤条件且被用户勾选的接口

### 2. 自定义字段

插件需要支持用户配置自定义字段，用于补充固定的反馈信息，例如：

- 反馈人-张三
- 环境-测试环境
- 项目-管理后台

用户配置后，这些字段需要在每次复制结果中自动带出，统一放在“自定义字段”区块中。

## 功能范围补充

- 支持读取当前页面基础信息，例如页面 URL、页面标题
- 支持展示当前页面相关接口请求历史
- 支持用户勾选一个或多个异常接口
- 支持提取接口的请求方法、接口地址及关键 Header 信息
- 支持提取接口请求时间与耗时信息
- 支持将整理后的反馈内容复制到剪贴板
- 支持在复制结果中附带用户配置的自定义字段
- 支持通过设置视图维护接口前缀过滤规则
- 支持在设置保存后提示用户刷新页面以应用新配置
- 支持在复制成功后给出明确提示
- 页面截图能力可作为可选功能，优先级可低于文本信息复制能力

## 建议交付范围

### MVP 范围

- 可读取当前页面标题与 URL
- 可采集当前页面接口请求记录
- 支持接口前缀过滤
- 支持勾选接口并复制标准化反馈内容
- 支持配置自定义字段
- 支持复制成功提示
- 支持设置保存成功提示及刷新页面提醒

### 可延期能力

- 页面自动截图
- 更复杂的接口筛选能力，例如按域名、状态码、关键词筛选
- 自定义字段排序、模板化管理
