# {{projectName}} — 技术架构

> 本文档记录项目的核心架构决策，是 AI 理解项目设计意图的主要依据。
> 由 `fe-set-ai-base` 初始化生成，请根据实际项目情况补充完善。

## 整体架构

{{architectureDescription}}

### 架构模式

- **模式**: {{pattern}}（如 MVC / MVVM / Clean Architecture / Feature-Sliced）
- **数据流**: {{dataFlowDescription}}
- **渲染策略**: {{renderingStrategy}}（如 CSR / SSR / SSG / ISR）

## 目录结构与职责

```
{{directoryStructure}}
```

| 目录 | 职责 |
|:-----|:------|
| `src/` | 应用源码 |

### 各层职责

- **UI 层**: {{uiLayerDescription}}
- **业务逻辑层**: {{businessLayerDescription}}
- **数据访问层**: {{dataLayerDescription}}

## 关键架构决策（ADR）

### ADR-001: {{决策标题}}

- **日期**: {{date}}
- **状态**: [提议 / 接受 / 已废弃]
- **背景**: {{为什么要做这个决策}}
- **选择**: {{选择了什么方案}}
- **结果**: {{选择后的影响}}

### ADR-002: {{决策标题}}

...

## 路由设计

{{routingDesignDescription}}

## 组件架构

- **通用组件**: `src/components/` — 可复用的 UI 组件
- **业务组件**: {{businessComponentPath}} — 业务逻辑相关组件
- **页面组件**: {{pageComponentPath}} — 路由页面组件

## 数据流

```mermaid
{{dataFlowDiagram}}
```

## 外部依赖

| 依赖 | 用途 | 版本约束 |
|:-----|:-----|:---------|
| {{package}} | {{purpose}} | {{version}} |

## 环境变量

| 变量 | 用途 | 必填 |
|:-----|:------|:----:|
| {{VAR_NAME}} | {{description}} | {{required}} |

---

> **维护说明**: 每次涉及架构变更时同步更新本文档。ADR 一经接受不应删除，已废弃的决策标注状态后保留。
