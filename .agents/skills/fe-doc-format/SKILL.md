---
name: fe-doc-format
description: 文档编写规范化 — 文档编制、需求文档、技术文档、接口文档、项目文档等场景下使用。当用户需要编写文档但未指定格式时，自动以中文编写并补充标准文档信息头。
---

# 文档编写规范

当用户提出编写文档需求，且未明确要求格式或语言时，遵循以下规范。

## 核心规范

### 1. 默认语言

一律使用中文编写文档，包括但不限于：

- 文档标题
- 章节标题
- 正文内容
- 图表标题
- 注释说明

除非用户明确要求使用其他语言，或文档内容本身包含专有名词（API 名、库名、变量名等）。

**标题与内容语言一致性**：若文档正文为中文，则文档标题必须为中文；若文档正文为英文，则文档标题必须为英文。当使用本技能修改文档时，应检查并修正标题语言以匹配正文语言。

### 2. 文档信息头

每份文档开头必须补充标准化的文档信息头，格式如下：

```markdown
# {文档标题}

> **编写时间**：{yyyy-MM-dd}；**使用模型**：{当前模型名称}；**用户**：{git user.name}

---
```

其中：
- **编写时间**：使用 `date +%Y-%m-%d` 格式，取当前日期
- **使用模型**：使用当前 AI 模型的名称（如 Claude Sonnet 4.6）
- **用户**：通过 `git config user.name` 获取，如果获取失败则显示"未知"

### 4. 文档修改规范

使用本技能**修改已有文档**时，除遵循上述编写规范外，还需执行以下操作：

- **同步 CR（代码审查）**：修改文档内容后，需对文档本身进行审查（CR），检查内容准确性、格式一致性及是否存在遗漏。若文档涉及项目代码或配置，还需同步审查相关联的源码文件。
- **标题语言修正**：检查文档标题语言是否与正文语言一致。若正文为中文但标题为英文（或反之），需将标题修正为与正文相同的语言。
- **信息头更新**：若文档已有信息头，需追加或更新"修改时间"字段，保留原始编写记录。

### 5. 触发场景

以下场景自动应用本规范：

- 需求文档 / 产品需求
- 技术方案 / 架构设计
- 接口文档 / API 文档
- 项目说明 / README
- 操作指南 / 用户手册
- 会议纪要 / 讨论总结
- 调研报告 / 技术选型

## 文档类型模板

### 需求文档

```markdown
# {项目名称} 需求文档

> **编写时间**：{yyyy-MM-dd}；**使用模型**：{模型名称}；**用户**：{git user.name}

---

## 背景与目标

{描述需求的背景、要解决什么问题、预期目标}

## 功能范围

### 核心功能

1. {功能一}
   - {子功能 / 详细描述}
2. {功能二}
   - {子功能 / 详细描述}

### 边界说明

- **包含**：{本次范围}
- **不包含**：{明确排除项}

## 功能详情

### {功能名称}

**描述**：{功能描述}

**交互流程**：
1. {步骤一}
2. {步骤二}
3. {步骤三}

**界面说明**：{界面描述或附图}

**验收标准**：
- [ ] {标准一}
- [ ] {标准二}
```

### 技术方案文档

```markdown
# {项目名称} 技术方案

> **编写时间**：{yyyy-MM-dd}；**使用模型**：{模型名称}；**用户**：{git user.name}

---

## 背景

{技术背景与动机}

## 方案设计

### 架构图

{架构描述或附图}

### 核心流程

{关键流程描述}

### 技术选型

| 模块 | 选型 | 理由 |
|:----:|:----:|:----:|
| {模块} | {技术} | {理由} |

## 详细设计

### {模块名称}

**数据结构**：

```typescript
interface Example {
  // 类型定义
}
```

**接口定义**：

```typescript
// API 或函数签名
```

## 风险与应对

| 风险 | 影响 | 应对方案 |
|:----:|:----:|:---------|
| {风险} | {影响} | {方案} |
```

### API / 接口文档

```markdown
# {服务名称} API 文档

> **编写时间**：{yyyy-MM-dd}；**使用模型**：{模型名称}；**用户**：{git user.name}

---

## 概述

{API 整体说明、基础地址、认证方式等}

## 接口列表

### {接口名称}

**请求方式**：`GET | POST | PUT | DELETE`

**接口路径**：`/api/v1/{path}`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|:------:|:----:|:----:|:----:|
| {name} | {type} | 是/否 | {说明} |

**响应示例**：

```json
{
  "code": 0,
  "data": {}
}
```
```

## 规范执行检查项

- [ ] 标题是否为中文（正文为中文时）
- [ ] 标题与正文语言是否一致
- [ ] 文档信息头是否完整（时间、模型、用户）
- [ ] 正文语言是否为中文
- [ ] 文档类型是否匹配模板结构
- [ ] 用户 `git config user.name` 是否已获取
- [ ] 修改文档时是否执行了 CR（内容准确性 + 格式一致性 + 遗漏检查）
- [ ] 修改文档时是否审查了关联的源码或配置文件
- [ ] 修改已有文档时是否更新了信息头中的修改时间
